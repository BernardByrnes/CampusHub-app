export default function StudentHome() {
  return (
    <main className="mx-auto flex min-h-screen w-full max-w-3xl flex-col justify-center gap-6 px-6 py-16">
      <p className="text-sm font-semibold uppercase tracking-[0.2em] text-slate-600">
        Production application foundation
      </p>
      <h1 className="text-4xl font-semibold tracking-tight text-slate-950">
        CampusHub
      </h1>
      <p className="max-w-xl text-lg leading-8 text-slate-700">
        The Next.js application boundary is ready. Student product surfaces will
        be recreated deliberately from the canonical prototype in a later phase.
      </p>
    </main>
  );
}
